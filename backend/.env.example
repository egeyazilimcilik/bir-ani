# MongoDB Atlas bağlantı adresi
# https://cloud.mongodb.com adresinden ücretsiz cluster oluşturun
# Connect > Drivers > Node.js > Connection string kopyalayın
MONGODB_URI=mongodb+srv://kullanici:sifre@cluster0.xxxxx.mongodb.net/bir-ani?retryWrites=true&w=majority

# JWT imzalama anahtarı (isterseniz değiştirin)
JWT_SECRET=bir-ani-secret-key-degistir

# Sunucu portu (varsayılan: 3000)
PORT=3000
